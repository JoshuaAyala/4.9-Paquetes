def suma(x,y, resultado):
	resultado = x + y
	return resultado

def resta(x,y, resultado):
	resultado = x - y
	return resultado

def circulo(radio, pi, resultado):
	resultado = pi*radio**2
	return resultado

def rectangulo(base, altura, resultado):
	resultado = base*altura
	return resultado